# start tmux on login
# WARNING: do not use with macOS terminal
#          it does not play well with tmux
# [[ $TERM != "screen" ]] && exec tmux # use only on LINUX

# promptline
# [[ -f ~/.promptline.sh ]] && . ~/.promptline.sh

# aliases
alias vimrc="vim ~/.vimrc && vim +PlugClean +qall && vim +PlugInstall +qall"
alias tmuxconf="vim ~/.tmux.conf && tmux source-file ~/.tmux.conf"
