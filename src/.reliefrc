# start tmux on login
# [[ $TERM != "screen" ]] && exec tmux # use only on LINUX
# WARNING: can cause macOS terminal to terminate on open

# aliases
alias vimrc="vim ~/.vimrc && vim +PluginClean +qall && vim +PluginInstall +qall"
alias tmuxconf="vim ~/.tmux.conf && tmux source-file ~/.tmux.conf"

# (COMING SOON: tmux-vim yank, tmux gutter, open files from tmux in current folder)

